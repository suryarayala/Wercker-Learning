build:
  box: node:4.5.0-slim

  steps:
    - npm-install
    - npm-test
    - script
      name: lint all the code
      code: |
        npm run lint
  after-steps:
    - slack-notifier:
        url: $SLACK_URL
        channel: notifications
        username: werckerbot
  
